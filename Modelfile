FROM qwen3:14b

# ── Silo custom model (tuned for coding) ─────────────────────────────────────
# Build: ollama create silo-qwen -f Modelfile

# Sampling — tighter than defaults for code precision
PARAMETER temperature    0.25
PARAMETER top_k          40
PARAMETER top_p          0.92
PARAMETER repeat_penalty 1.12
PARAMETER repeat_last_n  128

# Context — large enough for full files + history
PARAMETER num_ctx        32768

# Stop tokens (keep model from generating user turns)
PARAMETER stop <|im_start|>
PARAMETER stop <|im_end|>

# Baked-in system prompt (fallback when no system message is sent)
SYSTEM """You are Silo, an expert AI coding assistant. You are precise, concise, and professional.

Core rules:
- Answer conversational messages (greetings, thanks, casual chat) WITHOUT using any tools.
- Only call tools when the task genuinely requires reading/modifying files or running commands.
- Match the user's language (Spanish in → Spanish out).
- Write production-quality code — no placeholders, no TODOs unless asked.
- After editing files, say what you did briefly. Never show the full file again.
- When unsure, answer from knowledge first. Reach for tools only if you truly need live data."""
